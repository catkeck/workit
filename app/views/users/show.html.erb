<% provide(:title, @user.name) %>
  <div class = "friend_page">
    <div class="row">
      <aside class = "col-md-4">
        <section class="user_info">
          <h1>
            <%= gravatar_for @user, size: 50 %>
            <%= @user.name %>
            </br>
            </br>
            </br>
            </br>
          </h1>
        <% for friend in @user.friends %>
           <li><%= gravatar_for friend, size: 20 %>
           <%= link_to friend.name, user_path(friend) %></li>
           </br>
        <% end %>
       </section>
      </aside>
    <div class="col-md-8">
      <%= render 'add_friend_form' if logged_in? %>
        <% if current_user == @user && @weight.blank?%>
          <h3> Want to add your weight? </h3>
          <%= render 'shared/add_weight' %>
        <%elsif current_user == @user %>
          <h3> You are at <%= @weight.weight %> pounds. </h3>
          <h3> Want to update your weight? </h3>
           <%= render 'shared/update_weight' %>
        <% else %>
          <h3> <%= @user.name%>'s is currently at <%=@weight.weight %></h3>
        <% end %>
      <% if @user.microposts.any? %>
        <h3>Comments </h3>
        <section class = "micropost form">
        <%= render 'shared/micropost_form'%>
        </section>
        <ol class="microposts">
        <%= render @microposts %>
        </ol>
        <%= will_paginate @microposts %>
      <% end %>
    </div>
