<%= form_for(@weight, html: {multipart: true}) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :weight, placeholder: "Add weight" %>
  </div>
  <%= f.submit "Update Weight", class: "btn btn-primary" %>  
<% end %>
